default:
  tags:
    - autoscale

workflow:
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - when: always

stages:
  - format
  - test

Check Black format:
  stage: format
  script:
    - pip install black
    - black . --check --diff --color

Pytest:
  stage: test
  script:
    - pip install --upgrade pip
    - pip install pytest
    - pytest -vv
